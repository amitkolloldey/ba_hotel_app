build:
  docker:
    backend: Dockerfile
    frontend: frontend/Dockerfile
run:
  backend: /usr/sbin/nginx -g "daemon off;"
  frontend: npm run start
release:
  command:
    - php artisan migrate --force